#include <iostream>
#include <cassert>
using namespace std;


struct Node {
  int val;
  Node *next;
};

class Queue {
  Node *front;
  Node *end;
  int size;
  int numElements;

pu  Queue() {
    front = NULL;
    end = NULL;
    numElements = 0;
    size = 100;
  }

  bool isFull() {return (numElements == size);}
  bool isEmpty() {return numElements == 0;}

  void push() {int val} {
    Node *newNode = new Node;
    newNode->val = val;
    newN
    if (isEmpty())
      front = end = newNode;
    else {
      
    }
  }
};
