#include<iostream>
using namespace std;

void qsort(int *arr, int start, int end) {
  int index = (start + (end - start) / 2);
  int pivot = arr[index];
  swap(arr[start], pivot);
}

int main() {
  int arrr[] = {5, 1, 10, 2, 20};
  int size = sizeof(arr) / sizeof(arr[0]);
  qsort(arr, 0, size);
  return 0;
}
